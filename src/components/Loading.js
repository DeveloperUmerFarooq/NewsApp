import React from 'react'
import loading from '../loading.gif'
const Loading =()=>{
    return (
      <div>
        <img src={loading} alt='Loading Spinner' style={{backgroundColor:'transparent'}}/>
      </div>
    )
}
export default Loading
